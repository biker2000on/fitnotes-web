# TODOs

- Add Withings import/sync for body measurements.
- Update workout logs to have a similar workout summary as the new calendar workout summary
- calendar workout summary should at least be fullheight of the calendar widget. 
- load routine should never load the "Full Routine" only one day at a time. 
- select exercise and load routine in the header should only be available on the workout log page. 
- linking superset groups should allow to add to an exisitng group. Also, need to be able to name a superset. 
- need a mark all complete button and mark all complete per exercise button on workout view. 


More to come as the app gets used.
